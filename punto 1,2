# punto 1 Análisis de Ventas--Crear un dataframe de ventas mensuales, que incluya información de productos, meses y valor de ventas.
# Calcular el total de ventas por producto y mes
# Encontrar los productos más vendidos en cada mes
# Imprimir el total de ventas por producto y mes, y los productos más vendidos por 


# Crear un data frame de ejemplo con datos de ventas excel ventas
View(ventas)
# Convertir la columna 'fecha' a formato de fecha
ventas$fecha <- as.Date(ventas$fecha)

# Agregar una columna para el mes y el año
ventas$mes <- format(ventas$fecha, "%Y-%m")

# Calcular el total de ventas por producto y mes
total_ventas <- aggregate(monto ~ producto + mes, data = ventas, FUN = sum)

# Imprimir el total de ventas por producto y mes
print(total_ventas)

# punto 2 - Análisis de Datos de empleados-- Crear un dataframe de empleados que incluya el nombre, departamento, salario y género
# Calcular el promedio de salarios por departamento
# Calcular el porcentaje de empleados por género en cada departamento
# Imprimir el promedio de salarios por departamento y el porcentaje de empleados por género


# Crear un data frame de ejemplo con datos de ventas excel ventas
View(empleados)

# Calcular el promedio de salarios por departamento
promedio_salarios <- aggregate(salario ~ departamento, data = empleados, FUN = mean)

# Mostrar el resultado
print(promedio_salarios)

# Obtener los departamentos únicos
departamentos_unicos <- unique(empleados$departamento)

# Calcular el porcentaje de empleados por género en cada departamento
for (depto in departamentos_unicos) {
  empleados_depto <- empleados[empleados$departamento == depto, ]
  total_empleados <- nrow(empleados_depto)
  total_femenino <- sum(empleados_depto$genero == "F")
  total_masculino <- sum(empleados_depto$genero == "M")
  
  porcentaje_femenino <- (total_femenino / total_empleados) * 100
  porcentaje_masculino <- (total_masculino / total_empleados) * 100
  
  cat("Departamento:", depto, "\n")
  cat("Porcentaje de Femenino:", porcentaje_femenino, "%\n")
  cat("Porcentaje de Masculino:", porcentaje_masculino, "%\n")
  cat("\n")
}
